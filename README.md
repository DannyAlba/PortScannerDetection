# PortScannerDetection

This application takes in a PCAP file, and analyzes it for a potential port scanner. If it finds that an IP address is sending many syn requests, it sends back the IP in a report.
